{
  "name": "electronautoupdate",
  "version": "1.0.7",
  "description": "This is simple Electron app to test AutoUpadte functions",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-builder build --win --publish never",
    "deploy": "electron-builder build --win --publish always"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^8.5.1",
    "electron-builder": "^22.4.1"
  },
  "dependencies": {
    "electron-updater": "^4.2.5"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/JerryChenSyd/ElectronAppAutoUpdate.git"
  }
}
